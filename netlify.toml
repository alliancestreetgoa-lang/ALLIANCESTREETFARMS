[build]
  command = "pnpm install && pnpm --filter @workspace/alliance-farms run build"
  publish = "artifacts/alliance-farms/dist/public"

[build.environment]
  NODE_ENV  = "production"
  BASE_PATH = "/"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
